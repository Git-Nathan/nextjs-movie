import { Lato, Work_Sans } from "next/font/google";

export const workSans = Work_Sans({ subsets: ["latin", "vietnamese"] });

export const lato = Lato({ weight: ["400", "700"], subsets: ["latin"] });
